/**
 * About page - routes/about.tsx → /about
 */

import { defineComponent } from '@omnitron-dev/aether';

export default defineComponent(() => {
  return () => (
    <div>
      <h1>About Us</h1>
      <p>Learn more about our application</p>
    </div>
  );
});
