{
  "name": "@omnitron-dev/netron-http-core",
  "version": "0.1.0",
  "description": "Shared, environment-neutral HTTP fluent-interface primitives (cache manager, retry manager, query builder, request batcher) consumed by both @omnitron-dev/titan and @omnitron-dev/netron-browser. Single source of truth — eliminates the drifting per-package copies.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "netron",
    "http",
    "cache",
    "retry",
    "omnitron"
  ],
  "author": "Omnitron Dev",
  "license": "MIT",
  "sideEffects": false,
  "dependencies": {
    "@omnitron-dev/eventemitter": "workspace:*",
    "@omnitron-dev/netron-protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
