{
  "mcpServers": {
    "meridian": {
      "command": "meridian",
      "args": ["serve", "--stdio"],
      "env": {
        "RUST_LOG": "info",
        "MERIDIAN_CONFIG": "./meridian.toml"
      }
    }
  }
}
