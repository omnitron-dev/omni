/**
 * Configurable Proxy — re-export shim (browser).
 *
 * SHARED-HTTP-CORE: the implementation now lives in @omnitron-dev/netron-http-core
 * (single source of truth shared with @omnitron-dev/titan).
 */
export { ConfigurableProxy } from '@omnitron-dev/netron-http-core';
