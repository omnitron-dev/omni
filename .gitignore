internal/
.playwright-mcp/
# Dependencies
node_modules/

# Build outputs
dist/
*.tsbuildinfo

# Test & coverage
coverage/
.redis-test-info.json
file:*

# Turbo
.turbo/

# Environment & secrets
.env
.env.*
!.env.example

# Editor & OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Logs
*.log
logs/

# Debug
npm-debug.log*
pnpm-debug.log*

# Local config
.envrc
.npmrc
.mcp.json

# KB generated artifacts
kb/generated/

# Playwright output. `test-results/.last-run.json` was committed carrying
# `{"status": "passed"}` — a file asserting a result, tracked in git, for a
# suite that cannot run here at all (the browsers are not installed). An
# artifact that states an outcome is worse than no artifact: it answers the
# question without having done the work.
test-results/
playwright-report/

# Misc
*.tgz
*.map.gz
