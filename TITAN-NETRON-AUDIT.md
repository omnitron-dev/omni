# Titan / Netron / DAOS — Deep Audit & Remediation Tracker

> Mandate: industrial-grade reference architecture. Eliminate redundancy, inconsistency, legacy.
> Maximize reuse across all levels. Netron fully consistent + tested + red-teamed.
> **Process (MANDATORY):** investigate root cause → reproduce with test → root-cause fix → verify (no regressions). Never skip the test.
> Status legend: `[ ]` todo · `[~]` in-progress · `[x]` done · `[!]` blocked/needs-decision

---

## Audit phase status
- [x] **Phase 1** — Titan core (Nexus DI, application, cross-cutting) + Netron (RPC core, wire, auth red-team) + netron-browser + netron-react. (8 agents)
- [x] **Phase 2** — 14× titan-* modules (data/redis/cache/lock, events/scheduler, pm, notifications/ratelimit/telemetry, auth/metrics/health/discovery). (5 agents)
- [x] **Phase 3 AUDIT done** — DAOS Backends Architecture Uniformity Blueprint produced (bg agent). Backends are already ~85% uniform (shared `@daos/auth-utils` + `defineSystem` bootstrap; ZERO `@ts-nocheck`/legacy shims; ZERO version-suffixed service names). Canonical app = `apps/main`. Blueprint + roadmap below. **daos-app changes commit to the separate `internal/daos` repo.**

### PHASE 3 — UNIFICATION ROADMAP (from blueprint)
**✅ SESSION PROGRESS (daos commits 36741e9→c769fbf):** U-P0 DONE · U-P1 DONE (titan-kit + 6-backend migration + config-path-bug fix + wireNetronAuth + DAOS_REDIS_DB pinning) · cookie-mode `__Host-` hardening (omni b462cc9 + daos d1a1368). Next: **U-P2** (health/metrics unify). All changes per-app `tsc`-verified; framework source committed (rebuild propagates to backends).
- [x] **U-P0 cleanup — DONE:** deleted `apps/paysys/Oops.rej` (36741e9); storage `./bootstrap` export added (36741e9); geo `src/config/config.schema.ts`+`index.ts` added (c769fbf). (Stale `apps/storage/AUTH_MODULE_README.md` deletion still pending — low priority doc.)
- [x] **U-P1 — DONE (all sub-parts):** `@daos/titan-kit` + all 6 backends migrated (8943842) + config-path bug fixed; `wireNetronAuth` collapses the 6 duplicated bootstrap auth blocks (c449e1c); `DAOS_REDIS_DB` pins the Redis split in code (ae38748).
- [x] **U-P1 ✅ DONE (daos `8943842`) — all 6 backends on `@daos/titan-kit`; config-path bug FIXED:** every `shared/module-factories.ts` (+ paysys deposit-worker's 7th parser copy) now thin wrappers over titan-kit (parsers + parameterized Config/Redis/Database factories). Per-app specifics preserved via params (main gateway+audit, storage sessions, priceverse transform:false+migrations, geo timestamps:false+no-schema, paysys rotif+priceverse+rls, messaging forRoot-wrappers). **NET FIX:** titan-kit resolves config from absolute `appRoot` (+local.json+priorities) — fixes the latent relative-path config-load-under-supervision bug across 5 backends. All 6 apps `tsc --noEmit`: 0 errors. ~1000 lines deduped. **Remaining U-P1 sub-parts (next focused passes — both behavior-sensitive):**
  - **`wireNetronAuth()` (intricate — investigated):** auth wiring is duplicated near-identically across storage/priceverse/paysys/messaging; main adds `sessionLookup` (Postgres), geo is minimal (anonymous-first, no CSRF). BUT it spans TWO bootstrap hooks — `afterCreate` (createJwtAuthManager + `app.netron.configureAuth` + `setAuthBootState`) and `afterStart` (origin + CSRF middleware via `getAuthBootState`) — plus module-level `deferredPermVerRedis` and storage's token-transport handoff to custom routes. → extract `wireNetronAuthCreate(app, {jwtService, sessionRedis, logger, sessionLookup?, cnfHmacSecret?}) → bootState` + `wireNetronAuthStart(app)` into `@daos/auth-utils` (reuse its existing `set/getAuthBootState` + `boot-state.ts`). Security-critical → migrate per-app, behavior-preserving, tsc-verify each. Do as a dedicated pass (NOT a tail-of-session rush).
  - **`DAOS_REDIS_DB` code-pinning:** define a shared app→db map (main=0…paysys=4, geo=5) and pin each app's primary Redis `db` in code rather than relying on the `REDIS_URL` path convention (collision-safety). Behavior-sensitive (overrides URL-derived db) → confirm deployment expectation first.
- [ ] **U-P1 (orig design note, kept for reference):** create `@daos/titan-kit` (mirror auth-utils scaffold: type:module, peerDeps on titan-*, src/index.ts; daos workspace globs `packages/*`). API from the survey:
  - `parseRedisUrl`/`parseDatabaseUrl` — pure, BYTE-IDENTICAL across all 6 backends (+ paysys deposit-worker = 7 copies).
  - `createConfigModuleOptions({ app, envPrefix, schema, transform=true })` — varies only by prefix (MAIN_/STORAGE_/PAYSYS_/PRICEVERSE_/MSG_/GEO_) + path `apps/{app}/config/default.json` + schema.
  - `createRedisModuleOptions({ sessionsClient?, extraClients? })` — shared `sessions` client (SESSION_REDIS_URL||REDIS_URL db0) used by 5 apps; main adds gateway, paysys adds priceverse client.
  - `createDatabaseModuleOptions({ poolOverrides?, timestamps=true, audit? })` — geo timestamps:false; main ENABLE_DATA_AUDIT.
  - ALSO unify the inconsistent exported names (messaging: `createRedisModule` vs others' `createRedisModuleOptions`).
  - Migrate 6 apps → thin wrappers; needs `pnpm install` (daos) + per-app `tsc` verify. Then `wireNetronAuth()` in auth-utils + pin Redis DB via shared `DAOS_REDIS_DB` map.
  - **Execution note:** cross-repo + build-system + 6-app + per-app-verify → do as a focused build (not rushed), each app kept green.
  - ✅ **U-P1 part 1 DONE (daos `ead7092`):** `@daos/titan-kit` created (parsers + parameterized config/redis/db factories) + **main migrated** to thin wrappers (gateway client + data-audit preserved, app.module.ts untouched). `pnpm install` linked it; **main tsc 0 errors**. Proven end-to-end on the reference app.
  - ⏳ **Remaining U-P1 — DIVERGENCE MAP captured (rollout is a deliberate convergence, not a blind swap):**
    - **CONFIG — convergence is a NET FIX:** geo uses absolute `APP_ROOT`-resolved paths + `local.json` + explicit priorities, deliberately fixing a cwd-under-omnitron-supervision bug; main/storage/paysys/priceverse/messaging still use RELATIVE `apps/<app>/config/default.json` (the latent bug geo fixed). → titan-kit `createConfigModuleOptions` should take `appRoot` + adopt geo's robust pattern → FIXES the bug across all 6 (re-migrate main too). Also: priceverse omits `transform:camelCase`; geo passes NO schema.
    - **REDIS:** the `sessions` client (SESSION_REDIS_URL||REDIS_URL db0, `lazyConnect:false`, `maxRetriesPerRequest:1`) is canonical (5 apps); **storage is the OUTLIER** lacking logger-inject/readyLog/errorLog/healthCheck → converge it up. Extra clients: main=`gateway`, paysys=`rotif`+`priceverse`.
    - **DB:** vary by `timestamps` (geo:false), `migrations.directory` (priceverse/geo), `dataAudit` (main), `poolOverrides`; `healthCheck:true` canonical (storage lacks it).
    - **messaging:** divergent exported names (`createConfigModule`/`createDatabaseModule(options?)`/`createRedisModule(options?)`) + custom option types → keep wrapper signatures (app.module untouched) delegating to titan-kit.
    - **Plan:** enrich titan-kit API (appRoot+local.json+priorities config; transform; migrationsDir; timestamps; dataAudit; sessions client; extraClients) → re-migrate main (adopt config fix) + migrate 5 apps → per-app `tsc` verify → commit. Lockfile coupling ⇒ do as ONE coherent unit (all-or-nothing on the dep-adds).
    - **Bonus finding:** the relative-config-path bug in 5 backends is a real latent defect (config silently not loading under `omni start`) — U-P1's config convergence eliminates it. Add as its own tracked win.
- **PROPAGATION (corrected):** daos apps consume titan via `link:.../omni/packages/titan*` — BUT those packages' `package.json` `main`/`types`/`exports` point to `dist/` (built output, gitignored), NOT `src/`. So framework fixes go live in the backends after a package **build** (the committed SOURCE is what a deploy/CI `pnpm -r build` compiles); a stale local `dist` runs old code until rebuilt. Earlier "instantly live via link:" was imprecise. Net: source is committed (correct for deploy); rebuild the changed package locally to verify/run against it (e.g. titan-auth was rebuilt for the `__Host-` work below).

### COOKIE-MODE (HttpOnly) AUDIT + `__Host-` HARDENING (user-requested)
Audited daos cookie-mode auth (HttpOnly cookies replacing bearer, for multi-tab + max security). **Verdict: already advanced + best-practice** — `omni_access`(15m)/`omni_refresh`(7d) HttpOnly+Secure+SameSite=Strict; CSRF double-submit (`omni_csrf` non-HttpOnly, 256-bit `randomBytes`, `timingSafeEqual`); layered Origin enforcement (HTTP + WS); production fail-fast on non-Secure cookies; token-binding (cnf.fp HMAC → refresh-chain via main's sessionLookup); refresh rotation + reuse detection; cross-backend revocation (session-Redis permVer); no-JS-storage in cookie mode (NoopTokenStorage); multi-tab via whoami probe + storage events. wireNetronAuth now centralizes this wiring in ONE place.
- ✅ **Implemented the one gold-standard gap — `__Host-` cookie prefix** (RFC 6265bis): browser-enforced Secure + host-only + Path=/, defeating subdomain cookie-tossing / session fixation. Applied to the HttpOnly access+refresh credentials ONLY when Secure + host-only (transparent to clients — they never read these by name). Done CORRECTLY across set + clear: `buildSetCookie` fail-fast-guards the `__Host-`/`__Secure-` invariants; `buildClearCookie` is prefix-aware (forces Secure + Path=/ + drops Domain) so **signout actually deletes a `__Host-` cookie** (the subtle pitfall — a non-compliant clear is silently dropped, leaking the session). `createOmniCookieTransport` returns the resolved (prefixed) `accessCookieName`, threaded through daos `selectTokenTransport` so the CSRF middleware's "no cookie auth → skip" gate keys on the real name. +7 codec tests (36 pass). CSRF-cookie `__Host-` left for a coordinated client rollout (portal reads it by name) — tested follow-up.
- Files: omni `packages/titan/src/netron/auth/cookie-codec.ts` (+test), `packages/titan-auth/src/auth.token-transport.ts`; daos `packages/auth-utils/src/select-token-transport.ts`.
- [ ] **U-P2 module-consumption uniformity (sensible default = unify on Titan) — INVESTIGATED, next focused pass:**
  - **Health (contract-sensitive):** the 5 bespoke `modules/health` are already THIN titan-health ADAPTERS — e.g. storage's `HealthRpcService` injects titan `HealthService`, calls `.check()/.isReady()`, and converts to an app `HealthResponse` shape. So the dedup is the ~50-line adapter shell × 5 (`check`/`live`/`ready` + `@Public allowAnonymous` + `@RateLimit` + `convertStatus`/`convertToHealthResponse`). BUT it's CONTRACT-SENSITIVE: `@Service` name is `HealthService` (messaging/paysys/storage/priceverse) vs `GeoHealth` (geo), and return shapes vary (`HealthResponse`/`HealthCheckResult`/`GeoHealthResponse`) — all called by monitoring/LBs. → `createHealthRpcService({ name, toResponse? })` factory that preserves each app's `@Service` name + response shape; indicators stay app-registered with titan-health. priceverse/geo also gain the `titan-health` dep. Migrate per-app, preserve the wire contract exactly, verify.
  - ✅ **Metrics — ASSESSED, NO CHANGE (correct call):** all 4 (storage/paysys/priceverse/geo) ALREADY `extends` titan-metrics' `MetricsService` with a uniform DI wrapper (`TitanMetricsModule.forRoot` + `METRICS_SERVICE_TOKEN` override). They're already unified on the right substrate. Their domain methods are legitimately per-app STRONGLY-TYPED APIs (`recordDeposit(coin,status)` / `recordPriceUpdate()` / `recordUpload(bucket,status,bytes)`) — a generic `createDomainMetrics(prefix, defs)` factory would DESTROY that type safety/ergonomics (regression), and the only shared bit is ~8 lines of constructor boilerplate. Over-abstraction rejected; existing extend-base + typed-methods design is correct. (Minor cosmetic: geo injects `LOGGER_SERVICE_TOKEN`/`ILoggerModule` vs others' `LOGGER_TOKEN`/`ILogger` — left as-is.)
  - **PRODUCT DECISION (deferred to user):** main + messaging do NOT depend on titan-metrics. Adopting it is ADDING observability infra (not dedup) — a product/observability call, not a unification fix.
  - ✅ **storage auth shims — DONE (daos 39705b5):** the request-paradigm helpers (`getUserId(req)`…) were DEAD (0 callsites); deleted `auth/context.ts` entirely + migrated the 4 object/upload services to import `IAuthContext` directly from `@omnitron-dev/titan-auth` (the `AuthContext` alias was cosmetic). `get-current-auth.ts` already re-exports the ALS helpers from `@daos/auth-utils`. Also removed the stale `AUTH_MODULE_README.md` + its broken DOCKER.md link. storage tsc: 0 errors.
  - ⏳ **Remaining U-P2:** metrics `createDomainMetrics` (converge 4 `XMetricsService`); health `createHealthRpcService` factory (contract-sensitive, dynamic `@Service`); optional-dep reconciliation.
  - ✅ **Optional deps — ASSESSED, both KEEP (confirmed used):** messaging wires `titan-discovery` via `.forRoot()` in app.module; paysys uses `titan-pm`'s `CircuitBreaker`/`RateLimit`/`Supervisor`/`Child`/`SupervisionStrategy` primitives across 5 files (wallet-supervisor + BTC/XMR hot-wallets). Blueprint's "declared-unused titan-pm" was wrong — only the full `PMModule.forRoot()` supervision tree isn't enabled; the primitives ARE used. No change.
  - ✅ **Health — ASSESSED, NO HEAVY CHANGE (correct call):** read all 5 services. They already share the titan-health SUBSTRATE; the RPC adapters legitimately VARY — `@Service` name (`HealthService`×4 / `GeoHealth`), rate limit (1000 / messaging 120), delegation (priceverse/geo/messaging are thin pass-throughs to a domain `HealthService`; storage/paysys inline a converter), and response shape (`HealthResponse` / `GeoHealthResponse` / inline). The one true duplication is storage+paysys's `convertToHealthResponse`, BUT their `HealthResponse` types are part of the FRONTEND WIRE CONTRACT (exported via `./dto`), so sharing them = cross-package DTO coupling for ~40 lines. A dynamic-`@Service` factory would over-abstract legitimately-varied, contract-bound code (+ framework risk). Substrate already unified; thin adapters correctly differ → leave as-is.

**✅ U-P2 COMPLETE:** storage auth-shims removed (real fix); metrics + optional-deps + health all ASSESSED → already-appropriate (the discipline of NOT over-abstracting working, well-typed, contract-bound code). **The full unification arc U-P0 + U-P1 + U-P2 is now landed.**
- [~] **U-P3 naming/docs:** **portal-seed env-creds ✅ DONE (daos 54e2d31)** — config.ts no longer bakes in host/port/user/password for the 4 backend DBs; reads from env (SEED_DB_* → PG* → local-dev fallback), db names overridable, behaviour identical with no env set; tsc clean. REMAINING (deferred — WIRE-CONTRACT-SENSITIVE): one `@Service` naming convention (bare domain nouns, drop `Service` suffix + `OmnitronStorage*`/`PaySys*` prefixes) — the `@Service` name is the RPC identifier on the wire, so renaming needs coordinated client updates, not a blind sweep.
- Reference skeleton + per-app conformance matrix captured in the blueprint (session history).

---

## ★ NORTH STAR (user directive): UNIFIED ARCHITECTURE ACROSS ALL BACKENDS
Bring all 8 daos backends to ONE consistent Titan-based architecture — demonstrating Titan's
conceptual power for building manageable systems. This is the headline goal; framework
hardening (P0/P1/P2) is the foundation that makes the unified pattern trustworthy.
- **Phase 3 audit RUNNING (bg)**: per-app conformance matrix vs canonical pattern (service
  triplet, `@Service`/`@Public`, DI bootstrap, titan-module consumption, DTO exports) +
  cross-app duplication → shared package + prioritized unification roadmap.
- **Sequencing**: (1) finish high-value framework P1/P2 that the unified pattern depends on
  (AUTH-CONSOLIDATE, SHARED-PROTO, RESILIENCE-UNIFY, RATELIMIT-UNIFY — the canonical building
  blocks); (2) drive each backend to the reference pattern (likely `apps/main`), eliminating
  legacy/redundancy; (3) extract cross-app duplication to shared packages.
- **Repo note**: daos-app changes commit to the SEPARATE `internal/daos` git repo; framework
  changes to omni `main`.

## EXECUTION PRIORITY TIERS

### ✅ SESSION LOG (2026-06-12)
Closed (fix + tests + commit, all suites green): **SEC-2** (opt-in default-deny, wire+HTTP), **NET-14** (wire CALLs restricted to the @Public surface — discovered during SEC-2), **WIRE-11** (final-chunk-error hang — `_final` claimed `isClosed` and blocked `destroy`/`close`) + **local-peer** unexpose await-cascade (orphan stub after NET-2), **SEC-5** (per-peer refcounting for unref_service — cross-peer eviction DoS), **NR-2** (lazy self-stopping QueryCache GC — release blocker), **NR-3** (cache RAW, project `select()` per-observer — useQuery+useQueries), **NR-13** (`{stale}` filter respects per-query staleTime), **RL-atomicity** (atomic `checkAndConsume` Lua/memory for fixed-window), **NB-7** (read bearer from canonical `accessToken`), **NB-4** (per-peer reconnect factory + surviving attempt counter) + **NB-6** (real refresh + http-client re-invoke), **NB-5** (idempotency gate on both RetryManagers). Also cleared pre-existing reds surfaced en route (SEC-6 full-auth-flow, WIRE-11 writable-stream, NB-2 cache-invalidation, NB-8 backend-client guard) and captured precise designs for deferred work: **NET-14b** (property GET/SET surface), **NR-3b** (infinite-key namespacing), **RL-3b** (sliding-window + token-bucket Lua), **APP-3** (naive `EventBus.clear()` breaks tested persistent listeners — needs origin-aware clear), **NT-4** (user chose HYBRID — managed-client default + explicit override; bootstrap-DI + rotif multi-connection, focused pass). Suites: titan netron 3381/3381, titan HTTP 643/643, titan-ratelimit 14/14, netron-react 308/308, netron-browser 840/840. ~13 commits.
- Decisions taken (AskUserQuestion): NB-5 → safe default + `.idempotent()` opt-in; NT-4 → hybrid.
- Remaining: APP-1/2/3 (shutdown-engine unification + origin-aware listener clear — focused lifecycle pass, see lines 119-121), the 5 EPICs (multi-session), the rest of the #29 minor-cleanup BUNDLE (NET-6..13, WIRE-5/7/12/14..18, NX-6/10/12..16, APP-7..13, NB-9..19, NR-4..8, XC-*; titan-package items have slow build/verify cycles) + standalone PM-1/2/3, SC-3/4, RL-1.
- **Session 2026-06-13 (7 commits, each fix+test+verify):** NT-4 (hybrid managed-client + `InjectionInput` Nexus type fix), RD-1 (deleted deprecated up/down redis health — titan-health is the standard), NR-3b (infinite/regular cache namespacing via NUL-marked hash), NR-10/12/14 (useQueries barrel export / useSubscription unmount guard / drop client `as any`), NT-1 (revive dead event emitter — real token), NT-2 (in-app channel gets a redis client), NT-6 (rate-limiter in-memory fallback enforces all tiers, not just per-minute), RL-5 (HTTP limiter `trustProxy` secure-by-default). titan-notifications is now clean (NT-1/2/4/6). Suites green: netron-react 313/313, titan-notifications module 40/40 + channel/RL/decorator targeted, titan nexus 54/54, titan build 0-err.

### ✅ SESSION LOG (2026-06-11)
- **Phase 1 + Phase 2 audits COMPLETE** — ~130 findings catalogued below (titan core, Nexus, netron core/wire/auth, netron-browser/react, 14 titan-* modules). Architecture/structure defects are the P2 tier (god objects, duplication, dead code) + flagged inline.
- **MEM-FIX ✅** — corrected `daos_infra_invariants` memory (Nexus default is **Singleton**, not Transient — was inverted; verified registration.ts:69).
- **SEC-1 ✅ DONE + VERIFIED** — critical wire-path auth bypass fixed. New shared `auth/method-authorization.ts::enforceMethodAuthorization()` is the single source of truth; `RemotePeer.enforceMethodAccess` now enforces `@Public({auth})` on WS/TCP/Unix (was ACL-only/default-allow); HTTP middleware delegates to the same function (SEC-7 core). `AuthorizationManager.validateAccess` + new `validateAccessRequirements` (auth/utils) deduped. **3 reproducing tests** (wire-level-decorator-authz.spec.ts — confirmed RED before, GREEN after). **2292 auth/transport tests pass, tsc clean.**
- **SEC-3 ✅** — policy fail-closed now enforced on ALL transports via the shared function (was WS fail-open).
- **SEC-7 (core) ✅** — HTTP + wire enforcement unified into one function. Remaining: delete dead `websocket/auth.ts::authorizeMessage` + its duplicate readMethodMetadata/buildExecutionContext (cleanup).
- **NX-1 verified** — Singleton default confirmed; `defaults.spec.ts` pinning test still TODO (P3).

### P0 — Critical (security / data-loss correctness). Do first.
- [x] **SEC-1** ✅ FIXED+VERIFIED — `@Public({auth})` now enforced on WS/TCP/Unix via shared `enforceMethodAuthorization`. (method-authorization.ts, remote-peer.ts, utils.ts, authorization-manager.ts, http/middleware/auth.ts; test wire-level-decorator-authz.spec.ts)
- [x] **SEC-2** ✅ FIXED+VERIFIED — opt-in default-deny via new `Netron` option `authDefaultDeny` (default `false`, full back-compat). When enabled, a method gated by NEITHER a registered ACL NOR a `@Public({auth})` decorator is DENIED (FORBIDDEN) on BOTH transports: wire (`RemotePeer.enforceMethodAccess` step 3) + HTTP (`createAuthMiddleware` mirror, threaded from `server.ts`). New `AuthorizationManager.hasACL(serviceName)` predicate (+`IAuthorizationManager`); decorator-gated / `auth:false` / `allowAnonymous` / ACL'd methods unaffected (evaluated on own terms). Also closes the **call-any-instance-method** surface: `ServiceStub.call` dispatches by name with no public-surface whitelist, so an UNDECORATED instance method was wire-reachable via a known defId — under `authDefaultDeny` it is now denied (no ACL + no decorator). Test wire-level-default-deny.spec.ts (hasACL unit + default-allow back-compat + deny-mode decorator-pass/bare-@Public-deny/undecorated-deny); 321 auth-suite tests green; tsc clean. NOTE: the call-any-method surface remains default-ALLOW when `authDefaultDeny` is off — see NET-14 (always restrict wire calls to the `@Public` surface) for the non-opt-in fundamental fix.
- [x] **SEC-3** ✅ FIXED — policy fail-closed on all transports (shared function throws FORBIDDEN when policies declared but no PolicyEngine). Verified via http-auth tests + wire path.
- [x] **XC-1** ✅ FIXED+VERIFIED — serializer now round-trips a generic "extras" bag (subclass own-props) appended after `cause`, in BOTH titan + netron-browser serializers (lockstep — wire format is duplicated, XC-2/SHARED-PROTO). Subclass fields (serviceId/transport/requiredPermission/retryAfter/…) + `name` preserved. Test strengthened (titan-error-serialization.spec.ts: NetronError/TransportError/PermissionError/RateLimitError field survival). 3361 titan netron + 814 browser tests pass; tsc clean. NOTE: `instanceof` subclass identity NOT restored (data + `name` are) — deferred enhancement (needs name→ctor registry); folds into SHARED-PROTO.
- [x] **NET-1** ✅ FIXED+VERIFIED — `query_interface` auth-filtered path now returns `new Definition(id, peerId, filteredMeta)` (+parentId) instead of a `{...definition}` plain-object spread, so the msgpack type-109 class encoder recognizes it. Repro test asserts `instanceof Definition` (query-interface.spec.ts [NET-1]); integration tests exercise the real wire round-trip. 27 query-interface/integration tests pass; tsc clean.
- [x] **WIRE-1** ✅ FIXED+VERIFIED — `TcpConnection.processBuffer` now rejects a declared length > `maxPacketSize` (default 16 MiB) BEFORE buffering the body (destroys conn w/ PAYLOAD_TOO_LARGE). Covers Unix too (`UnixSocketConnection extends TcpConnection`); WS already bounded by `ws` maxPayload. New deterministic test tcp-packet-size-cap.spec.ts (4 cases incl. 0xFFFFFFFF). tsc clean.
  - **TEST-INFRA FINDING (new):** `vitest.config.ts` excludes ~10 transport test files (tcp/unix/ws-transport, transport-adapter/integration/isomorphic, error-serialization, cache-adapter, keep-alive-manager) because their shared `test-utils` transitively import the extracted redis module → those suites are DARK (don't run in CI). Explains the WIRE "no framing tests" gap. → New P2 task: fix test-utils import chain so transport tests run again. (Worked around here by a self-contained test file.)
- [x] **NB-1** ✅ FIXED+VERIFIED — browser packet serializer now gates stack (+cause stack) behind `includeStackTraces` (default false), mirroring titan T#38 + adding `setSerializerErrorOptions`. New test serializer-stack-suppression.test.ts (sender-frame marker absent by default, present when opted-in). 814 browser tests pass; tsc clean.
- [x] **NB-3** ✅ FIXED+VERIFIED — default auth storage changed `LocalTokenStorage` → `MemoryTokenStorage` (secure-by-default: tokens live in JS memory only; nothing written to web storage by default). `persistToStorage` wrote access + refresh token under `<key>_context` to localStorage (XSS-exfil). Persistence is now explicit opt-in (`storage`/`storageKey`). New test storage-default.test.ts (default writes nothing / explicit LocalTokenStorage persists). 817 browser tests pass; tsc clean.

### P1 — High (correctness / availability / broken features)
- [ ] **NET-2** `releaseInterface` recursion drops the `released` dedup set + fire-and-forget (unbounded recursion on cyclic parentId, unhandled rejections, mutation-during-iteration). → Thread set, await, snapshot.
- [x] **NET-14** ✅ FIXED+VERIFIED (CALL-scoped; discovered during SEC-2) — `ServiceStub.call` dispatched by method name straight to `this.instance[method]` with NO public-surface whitelist, so ANY undecorated instance method (lifecycle hooks, private helpers) was wire-callable by a peer holding the service's defId, even though it's not in `meta.methods` (the `@Public` surface). Confirmed `meta.methods` is built EXCLUSIVELY from `@Public`-annotated members (decorators/core.ts:585 `if (!isPublic) continue;`) and is always reliable for methods (read from the prototype — no instantiation). Fix: `RemotePeer.enforceMethodAccess` now takes a `kind` ('call'|'get'|'set') and, for `kind==='call'`, rejects any `methodName` not in `Object.hasOwn(meta.methods, …)` with a uniform NOT_FOUND (SEC-6 posture; `hasOwn` so inherited `toString`/etc. can't slip via prototype). Runs regardless of auth config (a non-`@Public` method is simply not API). Test wire-level-default-deny.spec.ts (undecorated method → NOT_FOUND in BOTH default-allow and deny modes; whitelist precedes the default-deny gate). Full netron suite green; tsc clean. **NET-14b (deferred):** property GET/SET are intentionally NOT whitelisted — `meta.properties` is extracted via `new target()` and is EMPTY for DI services whose constructor needs args, so a property whitelist would wrongly reject legitimate DI-service properties. Fixing needs property metadata re-extracted from the live instance at exposeService time. Also: the HTTP invoke path's ACL parity (it never calls `canAccessMethod`) is a separate observation, not NET-14.
- [ ] **WIRE-2** Readable-stream reorder buffer kills stream on a single gap ("backpressure" error) + no HOL gap timeout. Transports are ordered/reliable → reorder machinery is wrong/dead. → Assert in-order, drop reorder buffer (or fix gap semantics).
- [ ] **WIRE-6** `decodePacket` stream-info reads sit OUTSIDE try/catch (truncated stream packet → raw RangeError, not TitanError); no full-buffer-consumption check (trailing bytes ignored). → Wrap + assert consumption.
- [ ] **WIRE-10** Stream count limit checked AFTER allocation + throws from constructor in async `handlePacket` → unhandled rejection. → Check before create in handlePacket.
- [ ] **WIRE-11** WritableStream `_final`/`closeStream`/`destroy` double-teardown → can emit both EOS chunk + STREAM_CLOSE. → Single guarded teardown.
- [ ] **WIRE-13** `serializer.encode` monkey-patch registers StreamReference async then encodes sync → first-ever StreamReference encodes as plain object (race). → Register eagerly at init.
- [ ] **SEC-4** Token-cache revocation lag (cached AuthResult honored up to TTL after logout/ban); WS auth resolved once at connect, never refreshed for socket lifetime. → Document + force revoke on signout/ban; drop/re-auth live WS on revoke.
- [x] **SEC-5** ✅ FIXED+VERIFIED — `unref_service` (ungated core-task) deleted a SHARED dynamic sub-service stub on the FIRST unref, so peer A could evict a stub peer B still referenced (cross-peer eviction DoS). `enforceOwnership(def.peerId===caller)` does NOT work — dynamic defs are owned by the LOCAL peer, not the remote caller. Correct fix: per-peer refcounting. `ServiceStub` now tracks `refsByPeer` (+`addRef`/`releaseRef`/`totalRefs`); `LocalPeer.refService(instance, parentDef, callerPeerId?)` records the caller's ref (also on dedup hits); `LocalPeer.unrefService(defId, callerPeerId?)` releases ONLY that peer's ref and evicts only at total 0 (a peer that never referenced the defId is ignored with a warn). The core-task threads `peer.id`. The LOCAL authoritative release path (unexposeService / local releaseInterface, no peer id) still force-evicts (parent going away). `ILocalPeer` signatures updated to carry the optional `callerPeerId`. Test sec5-unref-refcount.spec.ts (2 peers share one stub → A unref keeps it for B → B unref evicts; a peer cannot evict a defId it never referenced). Full netron suite green; tsc clean.
- [ ] **NB-2** `CORE_TASK_AUTHENTICATE`/`CORE_TASK_INVALIDATE_CACHE` carry `netron.` prefix that doesn't match server bare task names → dead/broken constants. → Fix values to bare names; share constants.
- [x] **NB-4** ✅ FIXED+VERIFIED — ConnectionManager auto-reconnect was doubly broken: (1) on disconnect it scheduled a reconnect with a HARDCODED factory that always threw "Connection factory not provided", so reconnection could never succeed (just looped on the throw); (2) the attempt count was read from the just-removed `ManagedConnection` (always 0), so the exponential backoff never escalated and `maxAttempts` never tripped — an infinite baseDelay loop. Fix: a per-peer reconnect factory (`peerFactories`, registered via `addConnection(peerId, conn, factory)` / first `scheduleReconnect`) used by the disconnect handler, plus a SURVIVING per-peer attempt counter (`reconnectAttemptsByPeer`) that persists across removal, escalates the backoff, trips maxAttempts (→ `connection:reconnect_failed`), and resets on a successful (re)connect. cancelReconnect/stop clear the state. Test connection-manager-reconnect.test.ts (reconnect via factory; backoff escalates then gives up after maxAttempts — not infinite; no factory ⇒ no throwing loop). Full netron-browser 833/833 green; tsc clean.
- [x] **NB-6** ✅ FIXED+VERIFIED — the 401 path was inert two ways: (1) `attemptTokenRefresh` never called `authClient.refreshToken()` — it returned `false` PRECISELY when a refresh was needed and `true` (no-op) otherwise; (2) on "success" the ERROR-stage middleware only set an `auth:retry` metadata flag that NOTHING consumed, and `http-client.invoke` always re-threw the original error, so the request was never re-sent. Fix: `attemptTokenRefresh` now performs a REAL `authClient.refreshToken()` (the client owns the cookie/bearer refresh transport); `http-client.invoke` restructured into a single-re-invoke loop that, when an ERROR middleware refreshed auth and set `auth:retry`, re-sends ONCE with the refreshed headers/cookies (bounded by both the middleware's `maxRetries` and the client's `MAX_AUTH_REINVOKES`). On refresh failure → clearAuth + `onSessionExpired` + re-throw. Test auth-error-middleware.test.ts (real refresh + retry signal; failure clears auth; non-auth errors pass through). Full netron-browser 833/833 green; tsc clean. NOTE: scoped to the HTTP path (where the auth-error handler + 401 retry live); WS auth is connection-level (SEC-4).
- [x] **NB-7** ✅ FIXED+VERIFIED — `AuthenticationClient.extractTokenValue` read only `metadata.token` (and `context.metadata.token`), which the server never sends; the signin/refresh envelope carries the bearer token as `accessToken` (mirroring `refreshToken`, which `setAuth` ALREADY reads from `metadata.refreshToken`). So the access token was never stored → no Authorization header, no auto-refresh scheduling. Fixed: read `metadata.accessToken` first, falling back to the legacy `metadata.token` / `context.metadata.token` for back-compat. Cookie mode sends NEITHER (HttpOnly cookies), so it correctly returns undefined → client relies on cookies. Added the missing real-client coverage (the old `test/auth/client.spec.ts` is excluded by vitest `include: tests/unit/**`): tests/unit/auth/auth-token-extraction.test.ts (accessToken canonical, legacy fallback, cookie-mode no-token). Also cleared 2 PRE-EXISTING red tests surfaced by the run: cache-invalidation.test.ts asserted the old NB-2 `netron.`-prefixed task name (→ bare `invalidate_cache`); backend-client.test.ts crashed in NB-8 `forwardTransportEvents` on a mock without `on()` (→ guarded — degrade gracefully when the transport client isn't an emitter). Full netron-browser suite 827/827 green; tsc clean.
- [ ] **NB-8** `BackendClient` never forwards reconnect/disconnect events → `wireTransportEvents` dead → React `refetchOnReconnect` never fires. → Add `on()` forwarding.
- [ ] **NB-9** Leader-election: no liveness for frozen-but-alive leader; documented single-tab/always-leader fallback unimplemented (hard-throws on missing API). → Heartbeat + degraded fallback.
- [x] **NR-2** ✅ FIXED+VERIFIED (release blocker cleared) — `QueryCache` started a recurring `setInterval` GC in its CONSTRUCTOR, so the MultiBackend tests (which use fake timers and flush them) ran it 10000× → "Aborting after running 10000 timers, assuming an infinite loop". Fix: lazy, self-stopping GC — `ensureGcScheduled()` arms a `setTimeout` only when GC is enabled AND the cache is non-empty AND none is pending; each pass re-arms only while entries remain; an empty cache settles to ZERO pending timers (no forever-interval). Kicked from every unobserved-data insert (set/setError/hydrate); `clear()`/`destroy()` cancel it. `gcEnabled` config already provided the injectable off-switch. Verified the previously-RED MultiBackend bridge (provider/hooks/components, 88 tests) AND the cache-populating integration suites: full netron-react 304/304 green; tsc clean.
- [x] **NR-3** ✅ FIXED+VERIFIED (primary facet) — the shared QueryCache stored POST-`select` data, so two observers of one queryKey with different `select`s poisoned each other (the cache held whichever fetched first) and cache reads double-applied `select`. Fix: cache RAW data, project PER-OBSERVER. `useQuery` no longer applies `select` inside the deduped `executeFetch`; a stable `projectData` (selectRef-backed, so the cache subscription never re-subscribes on inline-`select` identity change) projects at every shared-cache read (initial state, fetch result, initial-fetch sync, subscription). `useQueries` mirrored via a `projectQueryData(query, raw)` helper at all 4 read sites + stores RAW at the write. Test useQuery.test.tsx: two observers w/ different select get their own projection from ONE deduped fetch; cache verified to hold RAW. Full netron-react 306/306 green; tsc clean. **NR-3b ✅ FIXED+VERIFIED:** infinite-vs-regular key collision (`useInfiniteQuery` stored `InfiniteData` under the same hash a regular `useQuery` uses → mutual clobber). Fix: infinite entries are stored under a NUL-marked hash variant (`hash + ' infinite'`) via a private `resolveHash(key, isInfinite)`; the 4 cache methods the infinite hook uses (`get`/`getQuery`/`set`/`subscribe`) take an `isInfinite` flag (default false ⇒ regular path byte-identical, zero regression risk), and `invalidate()`/`remove()` act on BOTH variants so invalidate-by-public-key still reaches the infinite query. Single map preserved (GC/findAll/matchQueryFilters unchanged — they key off `state.queryHash` = the marked key). Test `test/cache/infinite-namespace.test.ts` (5 cases: coexist, invalidate-both, remove-both, per-variant notify, regular-only unaffected). netron-react 313/313, tsc clean.
- [ ] **NR-9** `AuthProvider.login` is a stub that always throws (public via prism). → Implement or remove from type.
- [x] **NR-13** ✅ FIXED+VERIFIED — `matchQueryFilters({stale})` used `isInvalidated || Date.now() > dataUpdatedAt` — ALWAYS true (now is always after the last write), so `{stale:true}` matched EVERY query and a focus/reconnect `invalidateQueries({stale:true})` re-fetched the whole cache. The internal state didn't carry staleTime, so the matcher couldn't compute it. Fix: store effective `staleTime` on `QueryState` (captured from the writing observer) → thread through `set(key,data,staleTime?)`, `getOrCreateFetch(key,fn,staleTime?)` (set on success) → project onto the public `Query.state` (getQuery AND findAll's inline projection) → matcher: `isInvalidated || staleTime===0 || (staleTime!==Infinity && now > dataUpdatedAt + staleTime)` (0 ⇒ always stale per TanStack default + `QueryCache.isStale`; Infinity ⇒ never; undefined ⇒ 0, so legacy/SSR entries stay conservatively stale = no regression). Hooks pass their staleTime (useQuery/useQueries/useInfiniteQuery). Test stale-filter.test.ts (60s-fresh NOT stale, staleTime:0 stale, Infinity never, invalidation overrides). Full netron-react 308/308 green; tsc clean.
- [ ] **APP-1** Dual shutdown engines (manual loop in `_doStop` vs `ShutdownCoordinator`/`LifecycleController`) with divergent ordering/parallelism/reason depending on entry point. → Collapse to one (coordinator).
- [ ] **APP-2** Shutdown tasks run via different engine on `stop()` vs `shutdown()`; correctness hinges on one mutable bool. → Folds into APP-1.
- [ ] **APP-3** Event listeners leak across `restart()` — `EventBus.clear()` documents itself as called on teardown but has ZERO callers. ⚠️ INVESTIGATED: the obvious fix (`this._events.clear()` after the terminal emits in `_doStop`) is WRONG — it breaks documented/tested behavior. `application.spec.ts` ("state during restart") and `application.lifecycle.spec.ts` ("lifecycle with event emissions") register an app listener ONCE and assert it fires on BOTH restart cycles; a blanket clear drops USER listeners too, not just the per-start MODULE listeners that actually leak. Correct fix must distinguish origins: either (a) modules unregister their own listeners in `onStop` (symmetric lifecycle — audit each module), or (b) the Application snapshots the `_events` listener set before `start()` and removes only the delta added during that start cycle on `stop()` (needs an EventBus diff/restore API), preserving once-registered user listeners. Leave clear() UNCALLED until then — the leak only bites repeated `restart()` (rare) and is far less harmful than silently dropping user subscriptions. Folds into APP-1 (shutdown-engine unification).
- [x] **NB-5** ✅ FIXED+VERIFIED — the HTTP RetryManager's `defaultShouldRetry` retried ALL transient failures (5xx, timeout, connection-reset, generic errors), so `.retry(n)` on a non-idempotent RPC could silently double-execute a mutation after an AMBIGUOUS failure (request may already have been processed). Added an opt-in `RetryOptions.idempotent` (set via `.retry({attempts, idempotent:true})`): the DEFAULT condition now retries ambiguous failures (ECONNRESET/ETIMEDOUT, HTTP 5xx/408, SERVICE_UNAVAILABLE/REQUEST_TIMEOUT/INTERNAL_ERROR, unknown) ONLY when idempotent; failures that prove the request never reached the server (ECONNREFUSED/ENOTFOUND/ENETUNREACH) and an explicit 429/TOO_MANY_REQUESTS retry regardless; deterministic 4xx never retry. A custom `shouldRetry` bypasses the gate (caller owns it). Applied IN LOCKSTEP to BOTH RetryManagers (netron-browser + titan netron/transport/http — byte-identical files; folds toward SHARED-HTTP-CORE). User decision: safe default (chosen over default-idempotent-true / warn-only). Tests: retry-idempotency.test.ts (browser, 7) + a new gate describe in titan retry-manager.spec.ts (existing mechanics tests marked idempotent via defaultOptions; integration `.retry()` reads marked idempotent). netron-browser 840/840 + titan HTTP 643/643 green; tsc clean.

### P2 — Architecture / redundancy / reuse (high value, larger). Design once with full picture (await Phase 2/3).
- [ ] **SHARED-PROTO** Extract dependency-free `@omnitron-dev/netron-protocol` (packet types+Packet+encode/decode, serializer, Uid, Definition/Reference/StreamReference, error codes+hierarchy+factories, HTTP envelope/hint types, auth context types, core-task name constants, wire constants) consumed by BOTH titan & netron-browser. Kills NB-1, NB-2, NB-12, NB-15, NB-16, NB-17, XC-2 structurally. (Covers netron-browser duplication table — ~12 byte-identical/divergent units.)
- [ ] **SHARED-HTTP-CORE** Extract `@omnitron-dev/netron-http-core` (fluent query-builder, configurable-proxy, cache-manager, retry-manager, request-batcher) — 4 byte-identical files + NB-5/NB-13.
- [ ] **RESILIENCE-UNIFY** (EPIC — landscape mapped 2026-06-13) FOUR CircuitBreaker impls with INCOMPATIBLE APIs: (1) `utils/resilience.ts:148` — richest, EventEmitter, `execute(fn)`/`getState():CircuitState`/`getStats()` — the canonical candidate; (2) `errors/utils.ts:227` — generic `<T>`, `execute(fn)`/`getState():{state,failures}` — PUBLIC API of `@omnitron-dev/titan/errors` (documented), NO internal consumers → removal is a semver-breaking decision; (3) `nexus/mesh.ts:579` — in the experimental/dead mesh module (gated by NX-5 sign-off); (4) `netron/auth/policy-engine.ts:25` — a PRIVATE 4th copy used IMPERATIVELY (`isOpen()`/`recordSuccess()`/`recordFailure()`), AUTH-security-critical. Unification requires a canonical API serving BOTH execute-style AND imperative-style, then migrating policy-engine (careful — wrong state machine = auth fail-open/closed), deprecating the public errors copy, and deleting the mesh copy. Genuinely multi-session; not a safe partial. ORIG: ONE CircuitBreaker (3 copies: utils/resilience, errors/utils, nexus/mesh) + ONE backoff/retry primitive (4 copies: utils/retry, utils/backoff, utils/resilience, errors/utils) + ONE `HeartbeatMonitor` (3 health-check loops: ConnectionManager, BackendPool, KeepAliveManager) + ONE reconnect strategy (WIRE-4: BaseConnection vs ConnectionManager vs backend-pool). Covers XC-4, XC-5, WIRE-4, WIRE-8.
- [x] **ERROR-TABLE** ✅ DONE (titan side) — the per-code metadata was triplicated across `getErrorName`'s map, `getDefaultMessage`'s map, and `isRetryableError`'s list. Consolidated into ONE `ERROR_METADATA: Record<number, {name?,message?,retryable?}>` table (codes.ts); all three functions now read from it (behaviour preserved EXACTLY — codes.spec 26/26 unchanged, incl. fallbacks `ERROR_<code>`/`Error <code>` and `getErrorName(511)→ERROR_511`). Range-derived facets (`getErrorCategory`, `toHttpStatus`) deliberately stay as functions (they handle ARBITRARY codes, not just enumerated ones); the gRPC/JSON-RPC map is a contained transport.ts switch with a range default — left as-is. New `error-metadata-table.spec.ts` locks the single-source invariant (table drives all 3 + exact retryable set). titan errors 321/321, tsc 0-err. NOTE: netron-browser's error-code clone is XC-2 → SHARED-PROTO epic (separate)./isRetryableError/getErrorCategory/httpStatusMap/grpcStatusMap (6 drifting hand-maps). Covers XC-3, XC-9. Separate string identity from HTTP status.
- [ ] **SEC-7** Unify the 3 parallel authz implementations (HTTP middleware / WS authorizeMessage(dead) / binary ACL) into one — root cause of SEC-1/SEC-3. Delete dead `authorizeMessage` or make it the shared impl.
- [ ] **NET-5** ONE direction-aware `Marshaller` (encode/decode value) replacing 4 processValue/Args/Result pipelines (interface, service-stub, remote-peer, local-peer). Home for authz hooks so they can't be forgotten on a new path.
- [ ] **NET-3** Decompose `Netron` god object (1411 LOC): extract `PeerConnector`/handshake + move reconnect into ConnectionManager + `SequentialEmitter` for `emitSpecial`.
- [ ] **WIRE-3** Packet-native receive path: stop decode→re-encode→re-decode every packet (BinaryTransportAdapter round-trip). Forward decoded Packet directly.
- [ ] **WIRE-9** Fold multi-backend connection mgmt onto ConnectionManager; keep only routing+circuit-breaker+failover at multi-backend layer (two parallel connection stacks today).
- [ ] **NX-9/NX-11** Introduce typed `ContainerStore` injected into Nexus services (kills 8–11 positional-callback signatures; turns extracted procedures into real collaborators). 2488-LOC god object.
- [ ] **NX-2/NX-3** Collapse 2 parallel scoping impls (`resolveWithScope` vs `resolveRegistration`); fixes scoped multi-token cache-key bug (keyed by `context.scope` not `scopeId`).
- [ ] **NX-4** Scope-qualify `pendingPromises` key (cross-scope async resolution leaks one instance across scopes).
- [ ] **NX-5** Move/delete dead nexus `mesh`/`tracing`/`devtools` (3145 LOC, ~66 exports, zero internal consumers) off the main barrel; relocate the one needed `DependencyGraph` type.
- [ ] **NX-7/NX-8** Single conditional-provider model: keep context-aware `when`, default it Transient (else condition evaluated once & cached), promote to public `Provider` union; reconcile half-wired `condition`.
- [ ] **APP-4/APP-5** ONE metadata-key constants module (3 copies: core.METADATA_KEYS `nexus:*`, constants.DECORATOR_METADATA `titan:*`, injection-plan.KEYS); delete dead `titan:di:*`/`titan:service:*`; unify split-brained injection decorators.
- [ ] **NR-1** netron-react reimplements TanStack Query + Zustand + Jotai (~6 KLOC). → Adapter over `@tanstack/react-query` OR raise bespoke cache to TanStack bar (closes NR-3/4/5/6/11/13). Strategic — decide with user.
- [ ] **XC-8** Unify the 2 `ValidationError` + 2 `ServiceError` classes (one extends TitanError, one extends Error); ValidationEngine should throw canonical TitanError.
- [ ] **XC-2** netron-browser errors clone (folds into SHARED-PROTO).

### P3 — Medium/Low (cleanups, dead code, perf, tests, docs, memory)
- [ ] **MEM-FIX** Correct `daos-dev-principles` memory: Nexus defaults providers to **Singleton**, not Transient (NX-1). (verify first)
- [ ] **NX-1** Pin scope-default with tests (`defaults.spec.ts`); document default in JSDoc + README.
- [ ] **NX-6** Emit or prune ~19 declared-but-never-emitted LifecycleEvents; fix `addHook('onDispose')` never firing (ContainerDisposing).
- [~] **NX-10/12/13/14/15/16** — **NX-16 ✅ DONE**: `clearCache()` now also `this.pendingPromises.clear()`s, so an in-flight async resolution started before the clear can't re-cache a pre-clear instance (or hand it to a concurrent joiner), silently undoing the invalidation. Deterministic test added (container.spec "clears in-flight async resolutions too … (NX-16)"): a concurrent resolve started after clearCache must re-run the factory. Remaining (cosmetic): instances-cache scope guard, dup Middleware/ConfigToken types, isConstructor non-boolean, console.warn bypasses logger, untested guards.
- [ ] **APP-6** `@Service` instantiates class at decoration/import time (side effects, silent ctor failure). → Read metadata without instantiation.
- [ ] **APP-7/8/9/10/11/12/13** metrics uptime-unit divergence; replaceModule stale dedup; `off()` removes-all divergence; logger topo exclusion coupling; dual process-handler owners; simple.ts dup; minor dead exports.
- [ ] **NET-4** `refService` divergent signatures across peers (rename RemotePeer's → `registerRemoteDefinition`).
- [ ] **NET-6/7/8/9/10/11/12/13** dead no-op processArgs; wrong reflect key; dead exports/orphan JSDoc; interfaces barrel omits internal-types; disconnect not awaited; unexposeAll ignores async; methodCache not cleared; inline metadata read.
- [ ] **WIRE-5/7/12/14/15/16/17/18** binary heuristic misclassify; inconsistent close/error semantics across transports; KeepAliveManager global-singleton config collision; server send monkey-patch; byteLength probe; server close races; dead NamedPipe/NativeWS/bit-helpers; parseAddress IPv6.
- [ ] **SEC-6** Error messages distinguish not-found vs forbidden → service/method enumeration oracle. → Uniform responses; names to logs only.
- [~] **XC-3/6/7/10/11/12** — **XC-3 ✅ FIXED** (new `toHttpStatus(code)`: custom 600/601 + out-of-range → 500; used at both `TitanError` ctor + reset sites; test in codes.spec.ts). **XC-12 getAvailablePort ✅ FIXED** (searched a RANDOM OS port first, ignoring `startPort` — now searches sequentially from `startPort`; port-utils.spec.ts range tests tightened from the bug-tolerant `>=0` to actually assert the range). REMAINING: XC-6 prune dead error API (ErrorPool/Matcher/Logger/HandlerChain/adapters); XC-7 unbounded static stats in ctor; XC-9 AggregateError shadows global (public-API rename — breaking, defer); XC-10/11 untested domain/contract/formatting; XC-12 UUIDv7-doc nit (id.ts already correct — likely stale finding).
- [ ] **NB-10..NB-19** BroadcastChannel unvalidated; shared AbortController; envelope drift; dead request-batcher/services/pendingRequests; Uid triplicated; error hierarchy drift; packet impl drift; router dead branch; then-trap proxy.
- [ ] **NR-4..NR-14** subscription resync flood/no structural sharing; fetchData status-closure; useQueries stale closure; dead state/ + replaceEqualDeep + Stream types + dup InfiniteQueryOptions + QueryClientContext + netronClient + devtools placeholder; test gaps (subscriptions/SSR/middleware); barrel omits useQueries; GC ignores cacheTime; useSubscription cleanup setState; transport `as any`.
- [ ] **Test additions** across all areas (negative/adversarial): Nexus scope/disposal; netron auth cross-transport parity; TCP partial-read/framing; stream gap/reorder/limit; reconnect jitter; cross-transport wire-format; error subclass round-trip; titan↔browser enum equality.

---

## DETAILED FINDINGS BY AREA
(compact reference; full detail in session history. Format: ID · sev · file · problem → fix)

### Nexus DI (`packages/titan/src/nexus/`) — NX
- NX-1 high — registration.ts:69 — default scope is Singleton (memory says Transient — WRONG); untested → pin + doc + fix memory.
- NX-2 high — scoping.ts:175 — `resolveRegistration` scoped cache keyed by `context.scope` not `metadata.scopeId` → all scopes collapse to one bucket → fix key / delete method.
- NX-3 med — scoping.ts:22 vs :151 — two parallel scoping impls → collapse, route resolveAll through resolveWithScope.
- NX-4 med — container.ts:835/943/981 — pendingPromises keyed by token only → concurrent in-flight async resolution of the SAME scoped/request token in DIFFERENT scopes shares one promise → scope B gets scope A's instance. INVESTIGATED: the fix (scope-qualify the key) must be consistent across the container's 4 sites AND `asyncResolutionService.resolveAsyncDependencies` (which is handed `this.pendingPromises` and dedups dependency resolution by token too, args at container.ts:1003/1030). A partial qualification would be inconsistent. Entangled with the scope model (NX-2/NX-3 "2 parallel scoping impls") → fix in the scope-coherence pass. Narrow window (concurrent overlapping in-flight only; scope cache catches the rest).
- NX-5 med — index.ts:29/41/53 — mesh/tracing/devtools (3145 LOC) dead on public barrel → subpath or delete; relocate DependencyGraph type.
- NX-6 ✅ DONE (real-bug half) — `Container.dispose()` never emitted `ContainerDisposing`, so every `addHook('onDispose', …)` handler (which `addHook` maps to that event) was silently dead. `dispose()` now `await`s `lifecycleManager.emit(ContainerDisposing)` BEFORE teardown (emit isolates hook errors, so a throwing handler can't abort disposal). Test added in container.spec ("should fire onDispose hooks on dispose (NX-6)"); full nexus suite 904/904. REMAINING (cosmetic): the other ~18 declared-but-never-emitted `LifecycleEvent` enum members have no consumers/emitters — prune them in a cleanup pass (no behavioural impact).
- NX-7 med — types.ts:218 vs :740 — two conditional-provider mechanisms (`condition` half-wired vs `when`) → unify.
- NX-8 med — registration.ts:69 — `when` provider defaults Singleton → condition evaluated once & cached → default Transient.
- NX-9 med — container.ts:88 — 2488-LOC god object, services self-constructed, 11-arg callbacks → typed ContainerStore + DIP.
- NX-10..16 low — instances short-circuit scope guard; getRegistration[0]; clearCache leaks; isConstructor non-bool; console.warn; dup Middleware/ConfigToken; untested guards.

### Titan application core (`application/`,`lifecycle/`,`modules/`,`decorators/`) — APP
- APP-1 high — application.ts:578 vs shutdown-coordinator.ts:257 — dual shutdown engines.
- APP-2 high — application.ts:578 — stop() vs shutdown() task divergence.
- APP-3 high — event-bus.ts:113 — clear() dead → listener leak across restart.
- APP-4 high — core.ts:106 vs constants.ts:20 vs injection-plan.ts:23 — 3 metadata-key copies; dead `titan:di:*`/`titan:service:*`.
- APP-5 med — injection.ts:53 — injection decorators split across both key systems.
- APP-6 med — core.ts:606 — `@Service` does `new target()` at decoration time.
- APP-7..13 low/med — metrics uptime units; replaceModule dedup; off() removes-all; logger topo coupling; dual process handlers; simple.ts dup; minor dead exports.

### Cross-cutting (`errors/`,`validation/`,`utils/`,`tracing/`) — XC
- XC-1 **critical** — netron/packet/serializer.ts:91-205 — drops ALL TitanError subclass fields; test :105 codifies loss.
- XC-2 high — netron-browser/src/errors/* — clone of titan errors (manual-sync) → SHARED-PROTO.
- XC-3 high — codes.ts:62 + core.ts:105 — ErrorCode conflates HTTP status w/ identity; httpStatus wrong (600/601) for custom codes.
- XC-4 high — resilience.ts:148 + errors/utils.ts:227 + mesh.ts:579 — 3 CircuitBreakers.
- XC-5 med — retry.ts + backoff.ts + resilience.ts:629 + errors/utils.ts:173 — 4 backoff/retry impls.
- XC-6 med — errors/utils.ts + core.ts — dead API (ErrorPool/HandlerChain/Logger/Matcher/adapters/documentContractErrors).
- XC-7 med — core.ts:85,128 — unbounded static statistics mutated in every ctor; pool mutates readonly via `as any`.
- XC-8 med — errors/validation.ts:59 vs validation-engine.ts:53 — two ValidationError + two ServiceError classes.
- XC-9..12 low — non-exhaustive name/message maps; AggregateError shadows global; untested domain/contract/formatting; getAvailablePort ignores startPort, UUIDv7 doc says v4.

### Netron RPC core (`netron/` peer model) — NET
- NET-1 high — core-tasks/query-interface.ts:157 — returns `{...definition}` plain object not Definition → breaks type-109 encode on auth-filtered path.
- NET-2 high — abstract-peer.ts:342 (+local:196,remote:319) — releaseInterface drops released set, un-awaited, mutation-during-iteration.
- NET-3 med — netron.ts (1411) — god object (transport+connect/reconnect+registry+events).
- NET-4 ✅ DONE — `refService` lived on both peers with divergent signatures: `ILocalPeer.refService(instance, parentDef, callerPeerId)` (ref-count a local stub by instance) vs RemotePeer's `(def, parentDef)` (register a remote Definition by id). RemotePeer doesn't implement ILocalPeer (it extends AbstractPeer→IPeer base, which omits refService), so the shared name only type-checked via method-param bivariance. Renamed RemotePeer's → `registerRemoteDefinition` (+ internal caller in processResult + 2 tests). No interface change. titan tsc 0-err, peer tests 59/59.
- NET-5 med — interface.ts:241 + service-stub.ts:175 + remote-peer.ts:1063 + local-peer.ts:507 — 4 marshaling pipelines.
- NET-6..13 low — **NET-9 ✅ DONE**: `Netron.disconnect()` no longer fire-and-forgets `peer.disconnect()` (async on RemotePeer) — now `await`ed so the connection teardown completes before cleanup + the PEER_DISCONNECT emit, and a rejection can't escape as an unhandled promise (titan tsc 0-err, disconnect-cleanup 5/5). Remaining (verified NOT real bugs / cosmetic, low ROI vs slow titan build): `processArgs` is NOT a no-op (maps processValue); the `'service:metadata'` reflect read at interface.ts:270 is dead-but-harmless (the block throws unconditionally regardless); `$methodCache` is per-instance & method-bounded (not an unbounded leak). Genuinely-open cosmetics: dead exports/orphan JSDoc, barrel omits internal-types, unexposeAll async, inline metadata read.

### Netron wire (transport/packet/streams/multi-backend) — WIRE
- WIRE-1 **critical** — tcp-transport.ts:127 — unbounded buffer from length prefix; cap only after assembly.
- WIRE-2 high — readable-stream.ts:116 — reorder buffer kills stream on gap; no HOL timeout; reorder is dead complexity (ordered transports).
- WIRE-3 high — transport-adapter.ts:120 — decode→re-encode→re-decode every packet.
- WIRE-4 high — base-transport.ts:233 vs connection-manager.ts:666 vs backend-pool.ts:286 — 3 reconnect impls (only one jittered → thundering herd).
- WIRE-5 ✅ INVESTIGATED — practically unreachable + deliberate trade-off. The `0x7b`/`0x5b`→text heuristic (base-transport.ts:138) runs ONLY in the pre-handshake window; `if (this.binaryMode)` (line 92) short-circuits every packet after the first successful binary decode. A fresh connection's Uid sequence starts low, so the first binary packet's 4-byte-ID high byte is 0x00 — it can't be 0x7b/0x5b (that needs ID ≥ ~1.5 billion as the very first binary packet). The current T#48 logic deliberately treats `{`/`[` as text to avoid JSON handshakes coincidentally msgpack-decoding; "trying binary first" would regress that. A real fix is explicit text/binary framing → SHARED-PROTO/WIRE epic, not a safe standalone change.
- WIRE-6 med — packet/index.ts:222 — stream reads outside try/catch; no full-consumption check.
- WIRE-7 med — tcp 500ms vs ws 5000ms force-close; HTTP disconnect emits object not string reason.
- WIRE-8 med — connection-manager.ts:830 + backend-pool.ts:261 + keep-alive-manager.ts:182 — 3 health-check loops (T#50 guard copied 3×).
- WIRE-9 med — multi-backend/* reimplements connection mgmt, doesn't use ConnectionManager.
- WIRE-10 med — writable/readable-stream ctor:75 — stream limit after alloc + throws in async handlePacket.
- WIRE-11 med — writable-stream.ts:169 — _final/closeStream/destroy double-teardown.
- WIRE-12 med — keep-alive-manager.ts:51 — global singleton ignores per-conn config; pong-timeout leaks listener.
- WIRE-13 med — packet/serializer.ts:368 — StreamReference async-register race on first encode.
- WIRE-14..18 low — server send monkey-patch; byteLength probe; server close races; dead NamedPipe/NativeWS/bit-helpers/handleError; parseAddress IPv6.

### Netron auth red-team — SEC
- SEC-1 **critical** — remote-peer.ts:1283 + websocket/auth.ts:363(dead) — decorator auth unenforced on WS/TCP/Unix.
- SEC-2 high — authorization-manager.ts:140,201 — default-allow on no-ACL.
- SEC-3 high — websocket/auth.ts:487 — policy fail-open (HTTP fails-closed at http/middleware/auth.ts:256).
- SEC-4 med — authentication-manager.ts:438 — token-cache revocation lag; WS auth never refreshed.
- SEC-5 med — core-tasks/unref-service.ts:19 — unauthenticated/uncapped/no owner-check → cross-peer eviction.
- SEC-6 low — query-interface.ts:84 vs :121 + remote-peer.ts:1300 — not-found vs forbidden enumeration oracle.
- SEC-7 low — 3 parallel authz impls (root cause of SEC-1/3); dead authorizeMessage w/ passing tests = false confidence.

### netron-browser — NB
- NB-1 **critical** — packet/serializer.ts:51 — sends error stack traces to server.
- NB-3 **critical** — auth/client.ts:22 + storage.ts:26 — localStorage JWT+refresh token default.
- NB-2 high — core-tasks/authenticate.ts:23 — `netron.`-prefixed task names don't match server.
- NB-4 high — transport/connection-manager.ts:729 — broken auto-reconnect.
- NB-5 high — retry-manager.ts:282 — retries non-idempotent RPCs.
- NB-6 high — auth-error-handler.ts:341 + http-client.ts:183 — inert 401 refresh/retry.
- NB-7 high — auth/client.ts:201 — token extraction shape mismatch vs server.
- NB-8 high — multi-backend-client.ts:290 + backend-client.ts — reconnect events never forwarded.
- NB-9 high — leader-election/election.ts:81 — no frozen-leader liveness; fallback unimplemented.
- NB-10 ✅ INVESTIGATED — NOT a real issue. election.ts:70 forwards raw channel `data` to consumer `messageSubscribers`, but (1) leader role is driven entirely by the Web Locks API, NOT these messages — they cannot corrupt election state; (2) `broadcast(data)`/`onMessage(handler)` is a GENERIC same-origin pub/sub with no schema to validate against; (3) BroadcastChannel is same-origin by spec and the platform is closed/authenticated, so a hostile poster already implies XSS (game over regardless). An internal envelope/version filter would also break cross-version tab messaging mid-rollout (old tabs drop new-shaped messages). Left as-is by design.
- NB-11 ✅ DONE — `HttpConnection` held ONE shared `this.abortController`, reassigned per request: a request's timeout closure read the field at fire time (aborting whichever request wrote it LAST, not itself), and `close()` cancelled only that last one — while `ping()` never set the field, so its fetch carried `undefined`/stale signal and was uncancellable. Now every request/ping creates its OWN controller tracked in a `Set`; the timeout/finally close over the local const, and `close()` aborts the whole set. Test `tests/unit/transport/http-connection-abort.test.ts` (distinct per-request signals; close aborts all in-flight). netron-browser 842/842, tsc 0-err.
- NB-12 med — http/types.ts:27 — envelope types drifted (no conditional hints; lastModified type).
- NB-13 med — request-batcher.ts — fully impl + exported but never instantiated (dead, ~454 LOC).
- NB-14..19 low — dead services/pendingRequests maps; Uid triplicated; error hierarchy/factories drift + legacy.ts; packet impl drift; router dead branch+greedy prefix; then-trap proxy.

### netron-react — NR
- NR-1 high — package reimplements TanStack/Zustand/Jotai (~6 KLOC, zero deps on them).
- NR-2 high — cache/query-cache.ts:533 — multi-backend suite RED (GC setInterval under fake timers); newest bridge unverified.
- NR-3 high — hooks/useQuery.ts:159 — shared cache stores post-select data → cross-observer poison; infinite/regular key collision.
- NR-4 med — useQuery.ts:331 — subscription resync floods (no equality gate / structural sharing).
- NR-5 med — useQuery.ts:237 — fetchData closes over status → listener thrash, unstable refetch.
- NR-6 med — useQueries.ts:298 — refetch resolves with pre-fetch state (stale closure).
- NR-7 med — state/* + replaceEqualDeep + Stream* + dup InfiniteQueryOptions + QueryClientContext + netronClient + devtools — dead code.
- NR-8 med — tests missing for subscriptions/SubscriptionManager/SSR/useService/middleware/state.
- NR-9 med — auth/context.tsx:180 — login() always throws (public via prism).
- NR-10 ✅ DONE — public `index.ts` barrel now re-exports `useQueries` (+ its types); was reachable only via the deep `./hooks` path.
- NR-11 ✅ DONE (via NR-2) — GC now honours per-observer cacheTime (`observerCacheTimes` + `getEffectiveCacheTime`).
- NR-12 ✅ DONE — `useSubscription` guards the deferred state writes (`handleData` event callback + buffered-flush timer) with an `isMounted` ref and clears the pending flush timer on cleanup, so a late event/timer can't setState after unmount. (Dedicated unmount test belongs to NR-8's missing subscription harness; verified by inspection + full suite 313/313.)
- NR-13 low — cache/utils.ts:103 — stale check always-true → invalidate-all on focus/reconnect.
- NR-14 ✅ DONE — `getActiveClient()` drops the two `as any` casts; locals make the null-narrowing survive the `isConnected()` call (the fields are already typed `HttpClient|null`/`WebSocketClient|null`). tsc clean confirms the casts were noise.

---

## PHASE 2 — TITAN MODULES (added to priority tiers below; detail here)

### NEW P0 — Critical (from Phase 2)
- [x] **SC-1 (hazard eliminated)** ✅ — `onStart()` now FAILS FAST when `config.distributed.enabled` is true (was silently a no-op → duplicate cron on every node). Opt-in (default false), so single-node usage unaffected. 2 tests in scheduler-service.spec.ts; 43/43 pass. **Full distributed-scheduling implementation deferred to SCHEDULER-HARDENING (below).**
- [ ] **SCHEDULER-HARDENING (dedicated session)** — the scheduler has 10 interrelated findings best fixed as ONE coherent pass rather than piecemeal: **SC-1-full** (implement per-fire-window distributed lock over a shared store — add `acquireLock` to persistence; depends on resolving LK-1/LK-2 first OR a watchdog-free fire-window lock), **SC-2** (wire `saveJob` + guard load against unserializable closure targets; document that dynamic-handler jobs can't survive restart — only decorator jobs re-discovered each boot), **SC-3** (faked `updateNextExecution` `now+60000` → derive from node-cron), **SC-4** (racy `preventOverlap` — set isRunning atomically in executor), **SC-5** (node-cron v4 double-start + startJob leaks old task), SC-6/7/8/9/10 (empty-catch swallowing, metrics double-count, timeout-job registry leak, dead constants/unfired events). Reason for deferral: design-heavy (lock semantics, persistence serializability) + interrelated; piecemeal tail-of-turn fixes would violate the no-hacks bar.
- [x] **TR-1** ✅ FIXED+VERIFIED — `forwardEntries` now sends the full durable WAL contents (batch + un-acked backlog) and `clear()`s on full ack (mirrors retry/start). Added `await wal.flush()` before `readAll()` to fix a read-after-write race (append() writes async via T#69 chain). Self-contained 'both' mode also drains WAL after ingest. New test wal-clear-on-ack.spec.ts (clear-on-ack / no-duplicate-on-restart / backlog-not-lost). 10/10 telemetry tests pass. (per-entry dedup seq remains a noted enhancement.)
- [x] **DI-1** ✅ FIXED+VERIFIED — namespaced titan-discovery (3 tokens → `TitanDiscovery:*`) and titan-health (8 tokens → `TitanHealth:*`). Both confirmed real: omnitron defines its own `createToken('DiscoveryService')`; paysys already worked around the `HealthService` collision with `'AppHealthService'`. All consumed via exported constants → rename is transparent. Regression tests (token-namespacing.spec.ts ×2) assert namespaced ids differ from bare names. titan-health 122/122; titan-discovery namespacing tests pass; rename adds zero failures (verified via stash). Full sweep of remaining module tokens (titan-events `EventEmitter`, etc.) stays as P2 DI-TOKEN-NAMING.
  - **PRE-EXISTING FINDING (new, not mine):** titan-discovery has 28 failing tests — `DiscoveryService` lacks a `.start()` method the tests call (stale tests / lifecycle-API drift). Confirmed present before my change via git-stash. → P3: reconcile DiscoveryService lifecycle API with its tests.
- [x] **NT-3** ✅ FIXED+VERIFIED — `verifyWebhookSignature` now uses `crypto.timingSafeEqual` (length-guarded) instead of `===`. New test webhook-signature.spec.ts (valid/tampered-equal-length/different-length-no-throw/wrong-secret). 4 tests pass.
- [x] **NT-4** titan-notifications raw `new Redis()` bypassed titan-redis DI + app Redis-DB config (Invariant #3 FAIL → defaulted DB 0). **DONE (hybrid):** rate-limiter + preference-store providers (forRoot + forRootAsync) now optionally inject the managed `getRedisClientToken()` via `resolveNotificationsRedis()` — explicit `options.redis` still wins; `createTransport` derives Rotif's THREE dedicated connections (main/DLQ/pub-sub subscriber) from `managed.options` (sibling sockets, like `ioredis#duplicate()`) instead of hardcoded `localhost:6379`. rotif.ts stays a DI-free utility (it must own physically separate sockets). En route, fixed the underlying Nexus type gap: `inject:[...]` was typed `InjectionToken[]` but the resolver has always honored `{token, optional}` / `{token:'CONTEXT',type:'context'}` descriptors (forcing `as any` at call sites, e.g. logger.module) — added public **`InjectionInput`** union (nexus/types.ts) + threaded it through `Registration.dependencies` and `async-resolution.ts`. Also fixed 6 pre-existing stale-test reds in notifications-module.spec (`service.destroy()`→`onDestroy()`; warn test now spies the module logger, not `console.warn`). Verified: titan build 0 err, nexus core 54/54, titan-notifications tsc clean + module 40/40 + non-docker 80/80. (notifications.module.ts, rotif unchanged)

### NEW P1 — High (from Phase 2)
- [x] **LK-1** ✅ FIXED+VERIFIED — `withLock` now runs an auto-renew watchdog (default on): renews the lock every `floor(ttl/3)` via `extendLock` while `fn` runs, so a function outliving `ttl` can't let the lock expire (which let another node run the critical section concurrently). On renewal failure it stops + logs ERROR (JS can't preempt `fn`). Opt-out via `autoRenew:false`; timer `unref()`'d + cleared in finally. 2 tests (renews for long fn; no renew for fast fn / autoRenew:false). 61 lock tests pass; tsc clean. (LK-2 fencing tokens still open — deeper distributed-systems concern.)
- [x] **CA-1** ✅ FIXED+VERIFIED — `@Cacheable` now routes the miss-path through a per-method `Singleflight` (the in-package util it previously ignored), with an in-flight re-check. Concurrent misses for the same key dedupe → exactly one method execution + cache populate. New test: 20 concurrent callers → callCount===1. 19 decorator tests pass; tsc clean.
- [x] **RD-1** titan-redis shipped `redis.health.ts` (@deprecated, non-standard up/down format) yet `redis.module.ts` registered it live in BOTH forRoot + forRootAsync. **DONE (delete):** re-investigation found the task's premise stale — the deprecation pointed to a phantom `@omnitron-dev/titan/module/health` (never existed); the real standard indicators live in **`@omnitron-dev/titan-health`** (healthy/degraded/unhealthy), which daos storage already consumes. NO production consumer (omni or daos) imports titan-redis's deprecated `RedisHealthIndicator`/`HealthIndicator`/`HealthIndicatorResult` — daos apps import only `REDIS_MANAGER`/`RedisManager`/`IRedisClient`/`RedisService`. titan-health depends on titan (not titan-redis) + defines its own local `IRedisClient`, so wiring it back into titan-redis would invert layering. → Deleted `redis.health.ts`, unwired the providers + exports from `redis.module.ts`, dropped the 3 deprecated `index.ts` exports; deleted `test/redis.health.spec.ts`, fixed `redis.module.spec.ts` (dropped the export assertion) and rewrote `redis.integration.spec.ts`'s Health Monitoring block to exercise `RedisManager.healthCheck/isHealthy/ping` directly (the real functionality the wrapper delegated to). titan's own orphan `test/modules/redis|health/**` are already vitest-excluded. Verified: titan-redis tsc + build 0 err, module spec 12/12. (titan-redis only — no daos change needed)
- [ ] **EV-1** titan-events ships a SECOND inferior cron/scheduler (`EventSchedulerService`) duplicating eventemitter's own EventScheduler; its cron parser is FAKE (only `*/n` field-0, else falls back to "every hour" → `0 9 * * 1` becomes hourly). → delete, route through EnhancedEventEmitter scheduler / node-cron. (event-scheduler.service.ts:394)
- [x] **EV-2** ✅ FIXED+VERIFIED — wired `@Optional() @Inject(LOGGER_TOKEN) logger` into the 3 services that declared a `logger` field but never assigned it (EventsService — 14 dead log calls, EventSchedulerService, EventValidationService), mirroring EventBusService's working pattern. (metadata/history/discovery have no logger field — not dead.) New test logger-wiring.spec.ts (4 cases). 153 events tests pass; tsc clean.
- [ ] **SC-3** (scheduler EPIC) titan-scheduler `updateNextExecution` (scheduler.service.ts:341) fakes cron next-exec as `now+60000`; affects only the `nextExecution` DISPLAY metadata (health/sort) — actual firing is correct (node-cron owns it). INVESTIGATED: clean fix needs a cron-next calculator; the dep is `node-cron` (no next-time API) and `cron-parser` is NOT in the monorepo (can't cleanly add+install here; `yarn` absent). Hand-rolling a cron-next matcher risks DOM/DOW-OR + `L` + 6-field edge bugs (a subtly-wrong value is worse than an obvious placeholder). → add `cron-parser` in the dedicated scheduler pass. ORIG: `updateNextExecution` for cron is faked (`now+60000`); feeds health + sort key. → derive from node-cron task. (scheduler.service.ts:320)
- [ ] **SC-4** (scheduler EPIC) titan-scheduler `preventOverlap` guard is split across layers and self-referential: `service.executeJob` sets `markJobRunning(true)` (service.ts:290) BEFORE delegating to `executor.executeJob`, whose guard `if (preventOverlap && job.isRunning)` (executor.ts:80) then reads the flag THIS SAME tick just set (same job object by reference) — so the check tests its own write, not a prior run. Correct fix = single atomic check-then-set owned by ONE layer (executor as the sole execution gate is cleanest — both scheduled and manual `runJobNow` at service.ts:593 route through it), removing the service's pre-set; ripples to health/metrics consumers of `isRunning`. Belongs in the coherent scheduler pass. ORIG: `preventOverlap` guard racy/ineffective (isRunning set after dispatch) → overlapping interval/cron ticks both run. → set flag atomically at top of executor.executeJob. (scheduler.executor.ts:80)
- [ ] **RL-1** titan-ratelimit has ZERO tests over 3164 LOC incl. 3 algorithms with confirmed bugs. → full test suite (injected clock).
- [~] **RL-2** titan-ratelimit token-bucket refill: FIXED in titan-ratelimit via RL-3c (atomic `checkAndConsumeTokenBucket` carries the FRACTIONAL balance — Redis stores it as a string in a HASH so RESP/parseInt no longer truncates sub-token refill; memory keeps the float). Test asserts 4×25ms steps accrue exactly 1.0 token (no per-step floor-to-0 starvation). REMAINING: mirror the same refill-remainder fix in the SEPARATE netron `auth/rate-limiter.ts:611` token-bucket (folds into RATELIMIT-UNIFY).
- [~] **RL-3** PARTIALLY FIXED (FixedWindow/counter done) — added an atomic `IRateLimitStorage.checkAndConsume(key, limit, ttl)` primitive: Redis does the compare-and-increment in ONE Lua `EVAL` (PEXPIRE only on first creation, so a fixed window isn't extended), memory does it synchronously (no `await` between read and write). `FixedWindowAlgorithm.check` now uses it on the consume path, closing the get()+increment() TOCTOU (concurrent callers can no longer all pass `count<limit` then all increment). Also fixed a latent ttl unit bug there (was SECONDS passed to the ms storage API). Test ratelimit-atomicity.test.ts: 100 concurrent → exactly `limit` admitted, counter never overshoots; peek consumes nothing. **SlidingWindow ALSO DONE** — new `checkAndConsumeSlidingWindow` (Redis Lua: ZREMRANGEBYSCORE+ZCARD+conditional ZADD+PEXPIRE in one EVAL; memory: synchronous prune+count+add) closes its 4-await TOCTOU; `SlidingWindowAlgorithm.check` uses it on the consume path (+ same ttl-unit fix); tests for concurrency (exactly `limit`), algorithm-level concurrency, and window-pruning. **TokenBucket ALSO DONE (RL-3c)** — new `checkAndConsumeTokenBucket` (Redis Lua over a HASH: HMGET tokens/ts → refill `elapsed/windowMs*rate` capped at capacity → conditional consume → HSET + PEXPIRE, balance kept as a STRING so the fraction survives; memory: synchronous equivalent). `TokenBucketAlgorithm.check` routes BOTH consume and peek (cost 0) through it, so peek + consume share one storage shape; added an idle TTL (the old code set none → key leak). Tests: concurrency capped at capacity + fractional refill accrual (RL-2). **ALL THREE algorithms now have atomic check+consume in titan-ratelimit.** ONLY REMAINING: the separate netron `auth/rate-limiter.ts` token-bucket mirror (RATELIMIT-UNIFY).
- [x] **RL-5** ✅ DONE — netron HTTP `SlidingWindowRateLimiter` defaulted `trustProxy` to `true`, keying each client off the client-controlled `X-Forwarded-For`/`X-Real-IP`; an attacker could rotate the header per request for a fresh limit bucket each time (trivial bypass). Default flipped to `false` (rate-limiter.ts:118) — proxy headers are ignored unless the deployer explicitly opts in behind a trusted proxy; type doc updated with the security rationale (transport/types.ts). Test `test/netron/http-ratelimit-trustproxy.test.ts` (spoofed XFF shares one bucket by default; honored only when trustProxy:true). 4/4, no other test exercises this limiter. ORIGINAL NOTE: netron HTTP limiter `trustProxy:true` default → spoofed `X-Forwarded-For` = trivial limit bypass (titan-ratelimit correctly defaults false). → default false + trusted-CIDR. (http/rate-limiter.ts:118)
- [x] **NT-1** ✅ DONE — `NotificationsEventEmitter` injected the bare string `'EVENTS_SERVICE_TOKEN'`, which never matched titan-events' real `EVENTS_SERVICE_TOKEN` Token (created via `createToken('EventsService')`), so `@Optional()` left `events` undefined and every lifecycle event was silently dropped. Now value-imports `EVENTS_SERVICE_TOKEN` from `@omnitron-dev/titan-events` and `@Inject`s it. (notifications.events.ts)
- [x] **NT-2** ✅ DONE — InAppChannel was auto-registered as `new InAppChannel()` (no arg), so its `@Inject(REDIS_CLIENT)` never fired (decorators need container instantiation) → `this.redis` always undefined → in-app delivery inert. Both channel-registry factories (forRoot + forRootAsync) now resolve a client via `resolveNotificationsRedis(options.redis, managed)` (NT-4 helper — managed titan-redis client by default, explicit `options.redis` override) and pass it to the constructor; gate relaxed to managed-or-explicit. InAppChannel only runs commands + PUBLISH (never SUBSCRIBE), so the shared managed connection is safe. Test `test/inapp-channel-redis.test.ts` (inert without client / functional with / unavailable on ping-reject). tsc clean, module+decorators 67/67, channel 3/3.
- [x] **NT-6** ✅ DONE — RedisRateLimiter's in-memory fallback tracked/checked ONLY the per-minute tier, so a Redis outage silently let burst/hour/day be bypassed (e.g. 86400/day under a 60/min cap). The fallback now mirrors the Redis path: a shared `memoryTiers()` table drives both `recordToMemory` (increments every configured tier's window counter) and `getStatus` (checks all of them). Time tiers use boundary-aligned fixed windows identical to Redis; burst uses a fixed-window approximation of Redis' 1s sliding window (documented; may permit ~2× across a window edge — acceptable for a degraded fallback). `recordSent` now resolves limits and passes them down. Test `test/redis-rate-limiter-fallback.test.ts` (hour-binding, day-binding, burst-binding, all-clear). tsc clean, 4/4. (redis-rate-limiter.ts)
- [ ] **PM-1** titan core has orphaned duplicate PM test suite (~38k LOC) importing DELETED `src/modules/pm` (excluded via vitest). → delete `packages/titan/test/modules/pm/` + excludes.
- [ ] **PM-2** titan-pm `test/enterprise/` (15k LOC, 38% of test LOC) imports non-existent `src/enterprise/*` → fake coverage for saga/CQRS/actor. → delete or move to examples.
- [ ] **PM-3** titan-pm has 4 backoff impls + exports `computeBackoff` whose signature COLLIDES with titan core's `computeBackoff`. → standardize on RESILIENCE-UNIFY.
- [ ] **AU-1** netron AuthorizationManager ACL fail-OPEN while PolicyEngine fail-CLOSED (confirms SEC-2/SEC-3). → align fail-closed for closed platform.
- [ ] **MT-1** titan-metrics unbounded label cardinality via `pid` label + no series cap → memory growth / Prometheus blowup on flapping app. → drop pid label + LRU series cap + evict stale (app,pid).

### NEW P2 — Architecture (from Phase 2; merge with existing)
- [ ] **RATELIMIT-UNIFY** FOUR rate-limiter implementations (titan-ratelimit + netron auth/rate-limiter + netron http/rate-limiter + notifications RedisRateLimiter) → titan-ratelimit canonical; others become adapters. ~1500 LOC deletion; fixes RL-5 trustProxy + NT-6 fallback inconsistency. (folds RL-4)
- [ ] **AUTH-CONSOLIDATE** titan-auth boundary: KEEP JWTService + shared-session preset (used core); DELETE/relocate the ~840-LOC HTTP-auth stack (middleware/guards/decorators — AU-3, unused in omni, duplicates netron authz); pick ONE AuthContext (netron's `roles[]` vs titan-auth's `role`); move constant-time-compare/bearer-extract/isServiceRole (dup'd ~7×, AU-4) into one shared util.
- [ ] **EVENTS-CONSOLIDATE** 3 emitter layers (eventemitter EnhancedEventEmitter → EventsService → EventBusService) with byte-identical `compileWildcardPattern` dup'd 2× + scheduler subsystems each `new EventEmitter()` privately. → collapse; emit scheduler events through injected emitter. (EV-5 module 3× provider dup; EV-6)
- [ ] **HEARTBEAT-UNIFY (expanded)** Now 5+ periodic-probe/health loops (netron ×3 + titan-database health + titan-discovery heartbeat + titan-pm pool RPC + dead PM IPC heartbeat). Extract ONE `PeriodicProbe`/`withTimeout` primitive (re-entrancy guard + unref interval). Note: keep transport-liveness (socket ping) separate from titan-health indicator model — unify the SCAFFOLDING not the concern. (DB-2, DI-3, HE-3, PM-5)
- [ ] **RESILIENCE-UNIFY (expanded)** Backoff now reimplemented in: titan-redis (RD-2), titan-database (DB-1), titan-lock, titan-events ×2, titan-pm ×4 (PM-3), netron ×3. ONE backoff/retry primitive in titan/utils. CircuitBreaker now 5 copies (+ titan-pm decorator + pool). Unify.
- [ ] **DI-TOKEN-NAMING** Namespace ALL `createToken` names across shared packages (collision class — DI-1 is live proof); add dup-name-different-type warning to `createToken`. Also: titan-events bare `Symbol()` at events.module.ts:430-431 (only Symbol.for violation found); titan-pm un-namespaced `Symbol.for('process:metadata')` + plain-string metadata keys (PM-6).

### NEW P3 — Med/Low (from Phase 2)
- [ ] LK-2 fencing tokens; LK-3 withLock retry naming/release-race; LK-4 lock uses bare Error not titan errors.
- [ ] CA-2 write-back flush data-loss + unhandled rejection; CA-3 non-deterministic object-arg cache key; CA-4 auto-promote TTL skew; CA-5 no cross-instance L1 invalidation (doc); CA-6 dispose not lifecycle-wired (timer leak).
- [ ] RD-2 createRetryStrategy dual sentinel; RD-3 dead connectionPromises/createDynamicClientProviders; RD-4/5 destroyClient closeClient inconsistency, NOSCRIPT double-work.
- [ ] DB-2 health-check reentrancy guard; DB-3 nested-tx savepoints; DB-4 snake_case bookkeeping tables.
- [ ] SC-5 node-cron v4 double-start + task leak on restart; SC-6 empty-catch swallowing in executor/persistence/discovery; SC-7 metrics double-count + cancelled jobs uncounted; SC-8 timeout-job registry leak + decorator inheritance; SC-9/10 dead constants/events + forFeature dead Symbol tokens.
- [ ] EV-3 @EmitEvent decorator dead (never emits — `__eventEmitter__` never assigned); EV-4 empty-catch; EV-7 beginTransaction fake mock-handler; EV-8 dead constants/unfired events.
- [ ] PM-4 declare scope:singleton explicitly (relies on implicit default); PM-5 IPC heartbeat dead/unfinished wedged-child detection; PM-6 metadata-key namespacing; PM-7 onMessage/onLog no unsubscribe; PM-8 dead waitForProcessReady poll; PM-9 sanitizeEnv blocklist heuristic (strips legit AUTH*/leaks DATABASE_URL) → allowlist; PM-10 restartChild bypasses budget.
- [ ] NT-7 send() mutates caller options.channels; NT-8 double-delivery (transport+inline); NT-9 dead code (skipSuccessful/Failed no-op, cancel/getStatus stubs, validateSSL unusable, webhook bespoke retry); NT-10 KEYS blocking scan; NT-11 rate-limit consume-before-execute double-consumes on retry.
- [ ] MT-2 mutating metrics RPCs @Public(); MT-3 define() silent type-conflict.
- [ ] HE-1 all health endpoints anonymous incl check() probing Redis/DB (info-leak/DoS); HE-2 HealthService logger always undefined + @Injectable bypassed by useValue; HE-3 dup timeout-wrapper; HE-4 DatabaseHealthIndicator Kysely `raw()` API doesn't exist.
- [ ] AU-2 AuthMiddleware authenticate/required contract ambiguity; AU-4 triplicated constant-time-compare + dup bearer-extract; titan-auth ~7% test coverage (shared-session preset — most security-critical — ZERO tests).
- [ ] DI-2 health indicator `Token<any>`; DI-3 discovery heartbeat stale doc + no reentrancy guard; DI-4 eval vs evalsha per heartbeat.
- [ ] titan-metrics uses string `scope:'singleton'` instead of `Scope.Singleton` enum (style).

## PHASE 2 DETAILED FINDINGS BY MODULE
(format: ID · sev · file · problem → fix)

### titan-database/redis/cache/lock — DB/RD/CA/LK
All 5 invariants (Singleton, Symbol.for, bigint→Number, no-hardcoded-Redis-DB, camelCase) **PASS** for these 4. Severity order: LK-1 > CA-1 > RD-1 > CA-2 > DB-1/DB-2/DB-3, RD-2, CA-3, LK-2.
- LK-1 high — lock.service.ts:311 — no watchdog → expiry mid-section. LK-2 med — no fencing tokens. LK-3 low — retry naming/release race. LK-4 low — bare Error.
- CA-1 high — cache.decorators.ts:281 — no single-flight. CA-2 med — multi-tier-cache.ts:761 — write-back partial-fail loss + unhandled rejection. CA-3 med — :202 — JSON.stringify unsorted keys. CA-4 low — :330 — promote drops TTL. CA-5 low — no pubsub L1 invalidation. CA-6 low — dispose not lifecycle-wired.
- RD-1 high — redis.health.ts — deprecated dead but wired live. RD-2 med — redis.utils.ts:102 — own backoff + null/undefined dual sentinel. RD-3 low — dead connectionPromises:32 + createDynamicClientProviders:197. RD-4/5 low.
- DB-1 med — database.manager.ts:463 — own retry loop (no jitter) dup's @kysera/infra withRetry. DB-2 med — :259 — health setInterval no reentrancy guard. DB-3 med — transaction.context.ts:120 — nested tx no savepoint. DB-4 info — snake_case migration tables.

### titan-events + scheduler — EV/SC
- SC-1 CRIT, SC-2 CRIT, SC-3 high, SC-4 high (above). SC-5 med — scheduler.service.ts:193 — node-cron v4 double start + startJob leaks old task. SC-6 med — empty catch everywhere. SC-7 med — metrics.ts:63 double-count + cancelled uncounted. SC-8 low. SC-9/10 low — dead constants/unfired events (PAUSED/RESUMED/CANCELLED), forFeature dead Symbol().
- EV-1 high, EV-2 high (above). EV-3 med — events.decorators.ts:196 — @EmitEvent dead. EV-4 med — empty catch. EV-5 med — events.module.ts — providers declared 3× (decorator/forRoot/forRootAsync) with drift (async omits schemas+onError). EV-6 med — EventsService 1260-LOC re-wrap of EnhancedEventEmitter; compileWildcardPattern byte-identical to event-bus.service.ts:771. EV-7 low — beginTransaction fake. EV-8 low.

### titan-pm — PM
Core spawn/supervision/IPC is near industrial-grade (T#52-80 fixes real). Invariants 1 (worker/child_process) + 3 (Symbol.for) PASS; 2 (Singleton) implicit-only.
- PM-1 high, PM-2 high, PM-3 high (above). PM-4 med — pm.module.ts no explicit scope. PM-5 med — child-contract.ts IPC heartbeat fully spec'd, never consumed (wedged-child detection doesn't exist). PM-6 low — metadata namespacing. PM-7 low — onMessage/onLog no unsubscribe. PM-8 low — dead waitForProcessReady. PM-9 low/med — process-spawner.ts:541 sanitizeEnv blocklist (strips AUTH*/leaks DATABASE_URL) → allowlist. PM-10 low — restartChild bypasses budget. Verified SAFE: execSync pid interpolation (finiteness-guarded), replaceWorker spawn-before-kill, no shell:true.

### titan-notifications/ratelimit/telemetry — NT/RL/TR
- TR-1 CRIT, NT-1/2/3/4 high, NT-6 high, RL-1/2/3 high, RL-5 high (above). TR-2 low — truncateBefore/rotate dead.
- NT-5=RATELIMIT-UNIFY. NT-7 med — service.ts:117 mutates options.channels. NT-8 med — :156 double-delivery. NT-9 low — dead code. NT-10 low — KEYS scan. NT-11 low — retry double-consume.
- RL-4=RATELIMIT-UNIFY. RL-6 low — boundary edge cases. RL: titan-ratelimit CORRECTLY injects getRedisClientToken (the model). Bespoke `RateLimitExceededError extends Error` should be framework RateLimitError.

### titan-auth/metrics/health/discovery — AU/MT/HE/DI
Boundary map: titan-auth = JWTService(used) + shared-session preset(the bridge, daos-facing) + DEAD HTTP stack(AU-3). netron/auth = the RPC engine (Authn/Authz/Policy). Two incompatible AuthContext models (`role` vs `roles[]`).
- AU-1 high, AU-3 high, DI-1 high, MT-1 high (above). AU-2 high — middleware authenticate/required contract ambiguity (mooted if AU-3 removes). AU-4 med — 3× constant-time compare + 2× extractBearerToken + isServiceRole ×7.
- MT-2 med — rpc-service.ts:51 mutating RPCs @Public(). MT-3 low — registry.ts:128 silent type-conflict. (metrics-bridge↔titan-metrics verified CLEAN — the reuse model; APP-7 uptime units verified consistent.)
- HE-1 med — health.rpc-service.ts all anonymous incl check(). HE-2 med — health.service.ts:61 logger always undefined + useValue bypasses @Injectable. HE-3 low — dup withTimeout. HE-4 low — database.indicator.ts:162 Kysely raw() nonexistent.
- DI-2 low — Token<any>. DI-3 low — discovery.service.ts:37 stale doc + heartbeat no reentrancy. DI-4 low — eval not evalsha.
- titan-health NOT used by netron's 3 loops; titan-discovery duplicates omnitron discovery (DI-1 collision). titan-metrics is the clean reuse exemplar.
