/**
 * SHARED-PROTO: the `Reference` class now lives in
 * @omnitron-dev/netron-protocol — shared with the titan server. Re-exported
 * here so existing `core/reference.js` importers are unchanged.
 */
export { Reference } from '@omnitron-dev/netron-protocol';
